// This will display a ToolTip; user hovers over it for more info

/** Icon that displays a tooltip message on hover */
const ToolTip = ({ message }: { message: string }) => {
	return <div>Test {message}</div>;
};

export default ToolTip;
